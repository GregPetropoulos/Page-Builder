import React from "react";
import { SideNavbar } from "../components/SideNavbar";

export const PageView = () => {
  return (
    <div>
        <SideNavbar/>
        this is where the actual view of a page you have created. 
    </div>);
};
